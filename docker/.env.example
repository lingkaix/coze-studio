# Example environment for Docker Compose
# Copy to docker/.env (or project .env) and adjust as needed

# MQ provider selection
# - External NSQ (default in docker-compose): produce to nsqd and discover via nsqlookupd
MQ_URI=nsq://nsqd:4150?lookupd=http://nsqlookupd:4161

# - Lite mode (embedded, in-memory via Go CDK mempubsub)
#   Uncomment to use in-memory MQ (you can stop NSQ services when using this)
# MQ_URI=mem://

# Lite preset switch (optional). When set to 1, the app may override providers
# to embedded implementations; see lite plan docs.
COZE_LITE=0
